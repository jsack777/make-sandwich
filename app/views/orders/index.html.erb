<h1>Listing orders</h1>

<table>
  <tr>
    <th>Order Id</th>
    <th>Date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.id %></td>
    <td><%= order.updated_at.to_date if order.updated_at %></td>
    <td><%= link_to '', order, class: "icon-search" %></td>
    <td><%= link_to '', edit_order_path(order), class: "icon-pencil" %></td>
    <td><%= link_to '', order, class: "icon-trash", method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path %>
